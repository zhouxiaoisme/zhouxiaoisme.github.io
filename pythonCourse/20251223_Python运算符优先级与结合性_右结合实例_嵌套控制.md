									graph LR
										%% 三层嵌套：对应幂运算 2**3**2（外层[中间[内层]]）
										subgraph 外层
											A["主开关1（外层控制）"]
											subgraph 中间层
												B["主开关2（中间控制）"]
												subgraph 内层
													C["分开关（内层执行）"]
													D["执行器/负载"]
												end
											end
										end
										%% 连接关系：只有先确认内层生效，外层才会真正起作用（对应右结合运算顺序）
										C --- D
										B --> 内层
										A --> 中间层
										%% 样式匹配：三层嵌套设置不同深浅的蓝色填充色，从外到内逐渐变深
										style A fill:#f0f8ff,stroke:#3498db,stroke-width:2px
										style B fill:#f0f8ff,stroke:#3498db,stroke-width:2px
										style C fill:#f0f8ff,stroke:#3498db,stroke-width:2px
										style D fill:#f0f8ff,stroke:#3498db,stroke-width:2px
										style 外层 fill:#ffebee,stroke:#c62828,stroke-width:2px
										style 中间层 fill:#f3e5f5,stroke:#6a1b9a,stroke-width:2px
										style 内层 fill:#e8f5e8,stroke:#2e7d32,stroke-width:2px